# Route libc resolver traffic through systemd-resolved so NetworkManager can
# provide the common split-DNS/runtime resolver contract for downstream images.
L+ /etc/resolv.conf - - - - /run/systemd/resolve/stub-resolv.conf
